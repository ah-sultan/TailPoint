// import { ThemeConfig } from 'tailwindcss/types/config';

// const totalCol = 12;
// const _1colValue = 100 / totalCol;

// const tpCol = Array(12)
//   .fill()
//   .reduce((acc, _, index) => {

//     const colWidth = `${_1colValue * (index + 1)}%`

//     const makeCol = {
//       [`.tp--col-${index + 1}`]: {
//         flex: "0 0 auto",
//         "--tp-add-width" : `calc(var(--tp-gx) / ${12 - index})`,
//         width: `calc(${colWidth} - var(--tp-gx) + var(--tp-add-width))`,
//       },
//     };

//     return { ...acc, ...makeCol };
//   }, {});

// export const tpChildren = {
//   tpCol,
// };
