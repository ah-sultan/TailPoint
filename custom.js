
// const tpCol = Array(12).fill().reduce((acc, _, index) => {
//     const makeCol = {
//         [`col-${index + 1}`]: {
//             width: _1colValue * (index + 1),
//         }
//     };

//     return { ...acc, ...makeCol };
// }, {});

// console.log(tpCol)